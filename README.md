# Generating method sections for BIDS dataset

## CLI

usage:

```
chevron [-d DATA] [-p PARTIALS_PATH] [-e PARTIALS_EXT] template
```

example

```
chevron -d example/foo.json example/foo.mustache > example/foo.html
```
